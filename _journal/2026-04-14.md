# 2026-04-14 — Phase 0 investigation + v0.3.0 confidence-gated training

## Summary

Started the day with ~an hour of uncommitted classifier simplification work
(v0.2.0: binary SPAM/GOOD, single threshold, `-t` flag). That cut was
technically fine but addressed the wrong problem. Over the course of the day
the real root cause was identified, the plan was rewritten twice, v0.3.0
shipped a confidence-gated training pipeline, and the offline test loop
against `admin_maildir/` was validated end-to-end.

## Phase 0 — the training pipeline is disconnected

Original hypothesis from the plan: 215/216 users clustered near 5000/5000
is either a cap (inherited from Spamprobe's `maxHashTableSize`) or a
refreshable seed. Investigation rejected both.

- Spamprobe is **not** capped: `admin_maildir/.spamprobe.backup/sp_words` =
  880 KB (Jul 2025) vs `.spamprobe/sp_words` = 67 MB (Mar 2026). 77× growth
  under identical sieve wiring.
- Spamlite has no cap logic: read `src/storage.rs` top to bottom, `train()`
  is a straight `INSERT ON CONFLICT DO UPDATE`.
- Spamprobe's `-c` flag is **not** training mode: verified in
  `spamprobe.cc:625` it is just `setShouldCreateDbDir(true)`.
- Real sieve on mrn (`/etc/dovecot/sieve/global.sieve`) was `cat`'d and
  confirmed to call `spamfilter ... receive` for classification only.
  Training fires only on IMAP moves via `retrain-as-ham.sieve` and
  `retrain-as-spam.sieve`.

Root cause found in `spamprobe-1.4d/src/spamprobe/Command_receive.cc:104-121`:

> "receive — Scores message, prints score, and updates database accordingly."

Spamprobe's `receive` is unconditional TOE. `Command_receive::processMessage`
always calls `filter.classifyMessage(message, is_spam)` after scoring, which
writes token counts. **Spamlite's `receive` is implemented as spamprobe's
`score`** — pure classification, no DB update. That is the single biggest
functional difference between the two classifiers on this cluster. It
explains why `sp_words` grew 77× while `db.sqlite` stayed at 9 MB, why the
per-day stats JSONL shows corpus counts only moving on correction events,
and why the cluster is frozen at whatever the Spamprobe→spamlite migration
imported at baseline.

Cluster evidence from the 2026-04-14 digest confirmed the diagnosis:
multiple unrelated users show `spam=4929` exactly (cam@ck20, leathergoods,
walkenny) — fingerprint of a shared import baseline, not organic growth.
Good counts vary widely (2174, 3961, 4753, 5058, 29436) while spam clusters
tightly at 4900–5000 — asymmetric because users rescue fp from Junk (fires
`retrain-as-ham`, good grows) but rarely flag missed spam (retrain-as-spam
almost never fires). kingswaycomputers has 29436/4965 — decisive: even with
6× ham volume the spam side is still pinned at the plateau.

## v0.3.0 — confidence-gated training

Implemented spamprobe-style `receive` auto-training, but with a safety gate
instead of pure TOE. Pure TOE on a classifier with real fp/fn rates is a
self-reinforcing feedback loop: cam@ck20's 5 daily fp would get trained as
spam, producing more fp tomorrow. Confidence gate fixes this.

Changes in `src/main.rs`:

- Renamed old `cmd_receive` → `cmd_score`. The old behaviour (pure
  classification, no DB update) now lives under the correct name matching
  spamprobe's semantics. Kept for backward compat and read-only use.
- New `cmd_receive`: classify, print score to stdout first (preserves sieve
  contract), then train on self-verdict ONLY if score is outside the dead
  band `(gate, 1.0 - gate)`. Training failures are non-fatal stderr — mail
  delivery must not break because counts couldn't be written.
- New `-g GATE` flag + `SPAMLITE_TOE_GATE` env var, default `0.2`.
  Semantic: gate = trusted-region half-width. `gate=0.2` trains when
  `score ≤ 0.2 || score ≥ 0.8`. `gate=0.0` disables training. `gate=0.5`
  is pure TOE. Config-exposed from day one because the right gate width
  depends on the score distribution, which is an empirical question to be
  answered by the explain command once it exists.
- Renamed dispatcher entries: `receive` → new TOE variant, new `score`
  entry for the old behaviour. Usage text updated.
- Version 0.2.0 → 0.3.0.

Also earlier in the day (v0.2.0 portion, now folded in):

- `Verdict::Unsure` removed. Binary SPAM/GOOD.
- Collapsed `spam_threshold` (0.9) + `good_threshold` (0.1) into single
  `threshold` (default 0.5) with `-t` flag + `SPAMLITE_THRESHOLD` env.
- Dep bumps: `mail-parser` 0.10 → 0.11, `rusqlite` 0.32 → 0.38.

## Offline test loop against admin_maildir/

Validated end-to-end for the first time. Corpus sizes:
`Maildir/cur/` = 2531 messages, `.Sent/cur/` = 1548 (strong ham ground
truth), `.Junk/cur/` = 112 (known spam). `.spamlite/db.sqlite` starts at
5074/4947/255731 tokens per the live `.spamlite-stats.jsonl`.

Five-case gate test matrix against a sandbox copy of the db:

| Test | Setup | Expected | Result |
|---|---|---|---|
| A | default gate, 10 spam + 10 ham | +10/+10 | **+10/+10 ✓** |
| B | `-g 0`, empty DB, score=0.5 | blocked | **0/0 ✓** |
| C | default gate, empty DB, score=0.5 | blocked | **0/0 ✓** |
| D | `-g 0.49`, 10 spam + 10 ham | +10/+10 | **+10/+10 ✓** |
| E | `SPAMLITE_TOE_GATE=0.49`, 5 spam | +0/+5 | **+0/+5 ✓** |

Score stability: the admin_maildir messages all score at the extremes
(≤0.000006 for ham, ≥0.952 for spam). None land anywhere near the dead
band on the current corpus. This is good for baseline accuracy but does
mean TONE would be a near no-op on this cluster — confidence-gated TOE
is the right kickstart mechanism, not TONE.

Caught an inverted semantic in my own docstring during the test run:
I had documented `gate=0.0 = pure TOE` but the code (correctly) treats
`gate=0.0` as "nothing is trusted, no training" and `gate=0.5` as "trust
everything, pure TOE." Fixed before commit.

## mrn retrospective check — Phase 1.7 unblocked

Checked whether historical TRAIN events are log-reconstructible before
committing to "start logging from today and wait a month":

- `/var/log/mail.log` has 557 TRAIN events since 2026-04-12
- 5 rotated logs (`mail.log.1` through `mail.log.4.gz`) go back to
  2026-03-22
- At ~280 events/day cluster-wide, roughly 6000+ total historical events
- Sample format: `sieve: DEBUG: TRAIN: sandy@gardinermail.com.au -> good`
- Users' Archive/Trash folders still have the messages (shcv.com.au/admin
  has 5945 in .Archive/cur/, plus hundreds across other users)

Phase 1.7 changes from "month-long forward-logging project" to "week-long
backward-parsing project." Single highest-leverage investigation of the
day.

## Plan doc rewrites

The plan (`_doc/2026-04-14-spamlite-improvement-plan.md`) was rewritten
twice after receiving feedback:

1. First rewrite: promoted TOE/TONE from Phase 3 to a new Phase 0.5 after
   the Phase 0 root cause came into focus.
2. Second rewrite: replaced pure TOE with confidence-gated training after
   pushback about the feedback-loop risk; tightened the skew-detection
   rule to `max/min <= 2.5` used consistently in both Phase 0.5 wrapper
   exclusion and Phase 1.2 digest gate; added falsifiable-prediction
   requirement to Phase 2 items; reframed Phase 3.4 from "seed refresh"
   to "bulk Maildir ham training for cold corpora"; added per-JSONL
   version stamping to Phase 1.6; resolved Phase 1.7 with the mrn
   findings; added the "no TOE training in uncertainty band" invariant
   to operating principles.

## CLAUDE.md sieve section

Rewrote the deployment section after discovering the real mrn sieve.
Previous version referenced four sieve files including a non-existent
`train-as-ham.sieve`, and used stale names (`retrain-as-good.sieve`
instead of the real `retrain-as-ham.sieve`). New version documents the
three real scripts, the `spamfilter` / `spamfilter-retrain` wrapper
layer, and the hardcoded `-t 0.6` global threshold injection point.

## Explain command (Phase 1.4)

Implemented `classifier::classify_explain` returning a full `Explanation`
struct with per-token Fisher detail (good/spam counts, Robinson-corrected
f(w), top-N interesting set, H_spam/H_ham, P_spam/P_ham) alongside a new
`spamlite explain` CLI command that prints a readable breakdown.
Consistency test asserts explain's score matches classify's on a range
of inputs.

First run against admin_maildir/:

- Spam message (Portuguese digital-signature phishing): 42 tokens total,
  31 known, score 1.000000. Top tokens all Portuguese (`seu`, `análise`,
  `documento`, `assinatura`, `protocolo`) with perfect 0-good-N-spam
  discrimination. One counter-signal: `h:received:mail.renta.net`
  (474/127, f=0.216) because Received headers on the local server are
  mostly ham. The tokenizer handles Unicode correctly.
- Ham message (admin's own sent mail): 95 tokens total, ALL known to DB
  (zero unknown), score 0.000000. Top tokens are admin-identifying
  (`constable`, `markc@renta.net`, `0419`, `disha`) with 0 spam
  occurrences. Two weakly-spam-leaning tokens (`b:shocked`, `b:stick`)
  slip in but Fisher correctly aggregates.

Phase 2 prediction sanity-check: for the spam message, cutting
`max_interesting` from 150 to 27 would drop the 4 weakest signals
(`required`, `see`, `message`, `this`, `viewer` — all near f=0.54).
Unlikely to change the verdict. Confirms the Phase 2.1 change is safe
to attempt.

## Harvester binary (Phase 1.7 first pass)

Added `src/bin/harvest.rs` — dep-free, stdlib-only parser for dovecot
sieve TRAIN debug lines. Reads logs from argv paths or stdin, emits
JSONL `{t, u, v, sess}` records, prints summary by user and date to
stderr. Seven unit tests pass.

Harvested the full 31-day retrospective from mrn via
`ssh mrn 'zcat -f /var/log/mail.log*' | harvest`:

- **14,385 events parsed, 0 parse failures** across 2,094,049 log lines
- 94 unique users out of 216 (43% generated any correction at all)
- 31 days covered, 2026-03-15 to 2026-04-14
- Average organic rate ~287 events/day (excluding the 2026-04-06 bulk)

**Major new finding — the 2026-04-06 anomaly.** That one day had 5770
events, roughly 17× the normal daily rate. Looking at per-user totals,
thenutfarm has 6294 spam TRAIN events across the month, of which
approximately 5770 were on 2026-04-06. **thenutfarm's 2243/11222 corpus
skew is an artefact of that one-day script**, not organic user behaviour.
The threshold-override fix in Phase 1.3 compensates for the skew but
doesn't correct it; Phase 3.4 bulk ham training on thenutfarm's Archive
is the structural fix, promoted to Phase 1 urgency.

**User-behaviour taxonomy confirmed.** The 94 active users split into
three groups that map directly to the digest's fn/fp column:

- Heavy ham-rescuers (10 users, 50-1400 good events, 0-2 spam):
  kenelle, kate, brian, sandy, sky, lgq, jean, geckoarc, gympiegraphics,
  medicworx. These are the high-**fn** users.
- Heavy spam-flaggers (3 users, 100-6300 spam events, 0-6 good):
  thenutfarm, jaz@ck20, admin@coolasola. These are the high-**fp** users.
- Silent majority (122 of 216 users, 57%): zero or near-zero events
  either way. Corpus frozen at the migration baseline.

Causal chain is now fully explicit: user correction behaviour → per-user
corpus skew → classifier drift → digest fn/fp errors. Phase 1 operational
fixes compensate at the output; Phase 3.4 bulk training fixes the input.
Both are needed.

Not done yet (deliberate scope cut):

- Message-body correlation. The sieve debug line does not include the
  IMAP UID. A follow-up pass will need to cross-reference session IDs
  with nearby `imap(...)` log lines, or use per-user Maildir mtimes in a
  ±5-second window around each TRAIN event, to recover the actual
  message bytes. Feasible but out of scope for tonight's session.
- Gzipped log support. `zcat -f | harvest` handles this via shell pipe.
  Adding flate2 for inline gz reading is deferred until it actually
  becomes inconvenient.

## Commits

- `457519f` v0.3.0 — confidence-gated training + Phase 0 investigation
- `544483a` Add `explain` command for per-message token breakdown
- `867ddcd` Add spamlite-harvest — dovecot sieve TRAIN event log parser
- (pending) plan doc + journal updates with harvester findings

## Next session

1. Plan doc update with harvester findings (Phase 0 user taxonomy,
   Phase 1.3 thenutfarm root cause). → In progress at end of this session.
2. Message-body correlation pass for `harvest` — recover the (msg_bytes,
   verdict) benchmark corpus via Maildir mtime correlation. The
   retrospective dataset becomes fully usable at that point.
3. Shadow mode in the `spamfilter` wrapper (Phase 1.5) — half a day of
   bash in /usr/local/bin/spamfilter on mrn to log candidate-vs-prod
   divergence per user.
4. Empirical re-tune of the Phase 0.5 TOE gate default once explain has
   been run across a representative sample and we can see where the
   score distribution actually sits on this cluster's mail.
5. Canonical test case: run explain against cam@ck20's 5 fp messages to
   diagnose the balanced-corpus outlier. Needs the message bytes, which
   means (2) comes first.
